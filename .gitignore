# ── Rust build artifacts ──────────────────────────────────────────────────────
/target/
**/target/
**/*.rs.bk
*.pdb

# ── Coverage artifacts (scripts/coverage.sh) ──────────────────────────────────
coverage/
lcov.info
tarpaulin-report.html

# ── WASM compiled output ──────────────────────────────────────────────────────
# Only built artefacts; source *.wat files are tracked.
*.wasm

# ── Soroban / contract test snapshots ─────────────────────────────────────────
# Generated automatically by cargo test (soroban-sdk testutils); not needed in VCS.
**/test_snapshots/
test_snapshots/

# ── Node / TypeScript ─────────────────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
.npm/

# ── Bindings build output ─────────────────────────────────────────────────────
bindings/dist/
bindings/node_modules/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.*
.env.local
.env.*.local
.env.localnet
.env.deploy
deploy.env
deployment.env

# ── Deployer secrets (see docs/deployer-key-requirements.md) ──────────────────
*.secret
deployer.json

# ── IDEs / editors ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.DS_Store?
Thumbs.db

# ── Native build artifacts ────────────────────────────────────────────────────
*.o
*.a
*.so
.deps/

# Soroban test snapshots (generated by cargo test, not needed in VCS)
test_snapshots/
**/__snapshots__/
*.snap

# Generated / temporary files
*.tmp
*.log
*.pid

